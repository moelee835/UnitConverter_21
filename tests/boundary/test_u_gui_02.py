import pytest


def test_u_gui_02_from_to_unit_dropdowns(qapp) -> None:
    # Given: GUI 창 (headless QApplication, Mom Test S3·S4)
    # When: UnitConverterWindow 표시 후 from/to 단위 선택 위젯 검사 예정
    # Then: from·to 각각 meter/feet/yard 선택 UI (G1 — 오타 방지)
    pytest.fail("RED: U-GUI-02 — from/to 드롭다운 미구현, 의도적 실패")
